# Sistema LIA - Dependências Python
# ===================================

# Framework Web
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12

# Banco de Dados
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.30.0
psycopg2-binary>=2.9.0
alembic>=1.14.0

# Autenticação (fastapi-users)
fastapi-users[sqlalchemy]>=14.0.0
httpx>=0.27.0

# Admin
sqladmin>=0.20.0

# Cache / Redis
redis>=5.0.0

# IA / LLM
openai>=1.60.0

# Templates (usado internamente para geração de documentos)
jinja2>=3.1.0

# Geração de documentos
weasyprint>=63.0
python-docx>=1.1.0
aiofiles>=24.0.0

# Rate Limiting
slowapi>=0.1.9

# Configurações
pydantic-settings>=2.6.0

# Segurança (fastapi-users 14+ uses pwdlib internally for password hashing)

# Segurança (fastapi-users 14+ uses pwdlib internally for password hashing)
sse-starlette>=2.0.0
