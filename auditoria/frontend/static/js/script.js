document.addEventListener('DOMContentLoaded', () => {
    // This script is now empty as its functionality has been moved to other specific scripts.
    // The main index page now uses direct links to other pages.
});